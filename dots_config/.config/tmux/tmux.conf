unbind C-b

set -g prefix `
bind ` send-prefix

#hotkey
bind n new-window
bind w kill-pane

bind v split-window -h
bind s split-window -v


#mouse
set -g mouse on

#count from 1
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on


